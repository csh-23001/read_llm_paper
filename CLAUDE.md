# read_llm_paper — 仓库约定

本仓库是大模型相关论文的阅读笔记，**同时也是一个 Obsidian Vault**。在本仓库内进行任何写作、新建文件、整理笔记时，必须遵循以下规则。

## 1. 文件名全局唯一

- 整个仓库（vault）内的文件名（含扩展名前的 basename）**不允许重复**，无论位于哪个子目录。
- 原因：Obsidian 的 `[[wikilink]]` 按 basename 解析；重名会导致链接歧义。
- 新建笔记前先检查是否已存在同名文件（`find . -name "<name>.md"`）。若主题确实需要近似名称，请加上区分后缀（例如 `Attention.md` 与 `Attention-FlashAttention.md`）。
- 重命名时同步更新所有引用它的 wikilink。

## 2. 论文原文存放在 `_files/`（被 git 忽略）

- 论文 PDF / 原始文件统一放在仓库根目录的 `_files/` 下。该目录已在 `.gitignore` 中，不会随仓库分发。
- **笔记中严禁引用 `_files/` 下的本地路径**（既不要写 `[paper](_files/xxx.pdf)`，也不要用 wikilink 指向它）。本地路径对他人和未来的自己都无效。
- 论文引用一律使用**稳定的外部链接**，优先级：arXiv abs 页面 > 会议/期刊官方链接 > DOI > 作者主页 > 其他。每篇笔记顶部的 frontmatter 中至少要有一个外部链接字段（例如 `arxiv:` 或 `url:`）。

## 3. wikilink 中避免使用 `|`（alias 分隔符）

- Obsidian 的 wikilink 用 `|` 表示别名：`[[Note|显示文本]]`。但 `|` 同时是 Markdown 表格的列分隔符，**当 wikilink 出现在表格单元格内时，`|` 会被解析为列边界，链接被截断**。
- 因此**默认不要用 `[[A|B]]` 的别名写法**。如果希望显示文本不同于文件名，优先选择：
  1. 直接给目标笔记起一个合适的文件名（最干净）；
  2. 在目标笔记的 frontmatter 里加 `aliases: [...]`，然后用 `[[别名]]` 直接链过去；
  3. 实在需要 inline 别名时，**不要把这种链接放进表格**；放进表格的链接一律写成无别名的 `[[Note]]`。
- 同理，表格单元格里出现的字面量 `|` 必须转义为 `\|`。

## 4. 单篇论文一个笔记 + 方法外链化

- 每篇论文对应**一个独立的 Markdown 笔记**。
- **论文笔记的文件名一律使用论文的完整标题**（不要用缩写 / 项目代号 / Short-Name）。原因：标题全文最稳定、最易在 vault 内一眼识别、最不容易撞名。
  - 文件系统 / Obsidian wikilink 不允许的字符（`:`、`/`、`\`、`|`、`#`、`^`、`[`、`]`、`?`、`*`）按以下方式规范化：
    - 冒号 `:` 一律替换成 ` - `（前后各一个空格）；
    - 斜杠 / 反斜杠 / 竖线一律去掉或换成 ` - `；
    - 其他非法字符就近替换为空格，相邻多个空格合并成一个，并去除首尾空白。
  - 例：`"ELF: Embedded Language Flows"` → `papers/ELF - Embedded Language Flows.md`。
  - 项目代号、缩写（例如 `ELF`、`MDLM`）写进笔记 frontmatter 的 `aliases:` 列表，这样别处仍可用 `[[ELF]]` 链过去而不必引用全长文件名。
  - 仅当存在副标题歧义、同名论文等情况时才追加 `(Venue Year)` 之类的极小后缀消歧。
- 论文中提到的、可被多篇论文共享/比较的概念（方法、技巧、数据集、benchmark、架构组件等），应抽出为**独立的概念笔记**，在论文笔记中通过 `[[概念笔记]]` 链接过去；不要把同一个方法在每篇论文里重复展开。
- 概念笔记可以继续使用简短名字（如 `Flow-Matching.md`），它们的命名规则不受论文全称规则影响。
- 概念笔记里应记录该方法的核心定义、关键公式/伪代码、以及一个"被哪些论文使用 / 如何变体"的回链列表，方便横向比较。

## 给 Claude 的执行准则

- 创建任何 `.md` 文件前，先用 `find` 或 `grep` 确认 basename 不冲突。
- 创建论文笔记时，文件名按"第 4 条"规则用论文完整标题（替换掉 `:` 等非法字符），缩写写进 `aliases:`。
- 写论文笔记时，引用部分只使用外部 URL；如果用户给的是本地 PDF 路径，请在笔记中替换为对应的 arXiv / 官方链接（必要时询问用户该论文的外链）。
- 当笔记中出现一个跨论文复用的方法概念时，主动建议将其拆为独立笔记并改写为 `[[...]]` 链接。
- 不写 `[[Note|alias]]`，需要别名时改用 frontmatter `aliases`；表格里的 wikilink 一律无 alias，表格里的字面量 `|` 转义为 `\|`。
- 遵循 Obsidian Flavored Markdown（wikilinks、callouts、frontmatter properties 等）。
